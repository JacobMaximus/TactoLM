<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_tacto.xml
    Layout for TactoActivity — camera-based tactile navigation for deafblind users.

    Structure:
      ┌─────────────────────────────────────────────┐
      │  [tvSceneSummary]   small italic text, top  │
      │  [scrollView]       growing card list        │
      │       └─ [cardContainer] LinearLayout        │
      │  [btnScan]          large white circle, btm  │
      └─────────────────────────────────────────────┘
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/tacto_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_screen">

    <!-- ── Scene summary text ────────────────────────────────────────────── -->
    <TextView
        android:id="@+id/tacto_tv_scene_summary"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginTop="48dp"
        android:layout_marginEnd="20dp"
        android:fontFamily="sans-serif"
        android:textSize="13sp"
        android:textStyle="italic"
        android:textColor="#CCFFFFFF"
        android:lineSpacingMultiplier="1.35"
        android:gravity="center"
        android:visibility="invisible"
        android:text=""
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ── Detected-object card list ─────────────────────────────────────── -->
    <ScrollView
        android:id="@+id/tacto_scroll_view"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="24dp"
        android:scrollbars="none"
        android:overScrollMode="never"
        android:fillViewport="false"
        app:layout_constraintTop_toBottomOf="@id/tacto_tv_scene_summary"
        app:layout_constraintBottom_toTopOf="@id/tacto_btn_scan"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:id="@+id/tacto_card_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="4dp"
            android:paddingBottom="4dp" />
    </ScrollView>

    <!-- ── SCAN circle button ─────────────────────────────────────────────── -->
    <!--
        The button is drawn as a 180dp circle with a white background.
        We use a FrameLayout so the TextView sits centred inside the circle.
    -->
    <FrameLayout
        android:id="@+id/tacto_btn_scan"
        android:layout_width="180dp"
        android:layout_height="180dp"
        android:layout_marginBottom="16dp"
        android:background="@drawable/tacto_bg_scan_button"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackgroundBorderless"
        app:layout_constraintBottom_toTopOf="@+id/nav_bar_include"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tacto_tv_scan_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="SCAN"
            android:textSize="26sp"
            android:textStyle="bold"
            android:textColor="#0A0A0A"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="0.08" />
    </FrameLayout>

    <!-- ── FLOATING BOTTOM NAVIGATION PILL ───────────────────────────── -->
    <!-- Wrapped in FrameLayout so layout_gravity/margins on the pill work
         correctly inside a ConstraintLayout parent -->
    <FrameLayout
        android:id="@+id/nav_bar_include"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <include layout="@layout/layout_nav_bar" />

    </FrameLayout>

    <!-- ── Loading spinner (visible while awaiting Gemini response) ──────── -->
    <!--
        Pinned to all four parent edges so ConstraintLayout centres it.
        visibility="gone" at rest — TactoActivity shows/hides it around the
        Gemini API call.
    -->
    <ProgressBar
        android:id="@+id/loadingSpinner"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminateTint="@android:color/white"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
